const userRole = {
	SUPERADMIN: 'SUPERADMIN',
	USER: 'USER',
};

const userStatus = {
	ACTIVE: true,
	INACTIVE: false,
};

const tokenType = {
	EMAIL_VERIFICATION: 'EMAIL_VERIFICATION',
	PASSWORD_RESET: 'PASSWORD_RESET',
};

export default {
	userRole,
	userStatus,
	tokenType,
};
